Main was created with balance starting in $1.000 as required. 
Main creates operations history as a list to add the information of the opperations that would be involved in the usage of the program.
Main calls the menu function from menu.py and executes it. 
Menu function ran in main.py and sents the variable "balance" and the list operationsHistory as parameter to connect it with the other files in the directory. 